@startuml
start
:Drive increments continuously;
if (Drive <= Threshold) then (yes)
    :Satisfied;
else (no)
    :Unsatisfied;
    repeat
        fork
            :Searching;
        fork again
            :Drive increments continuously;
        end fork
        if (Partner Found?) then (yes)
            :Engage Partner;
            if (Successful Exchange?) then (yes)
                :Drive decrements;
                if (Drive < Threshold) then (yes)
                    :Satisfied;
                else (no)
                    :Continue Engaging;
                endif
            else (no)
                :Resume Searching;
            endif
        else (no)
            :Continue Searching;
        endif
    repeat while (Drive > Threshold)
endif
stop
@enduml